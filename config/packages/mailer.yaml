framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'
        # Default sender applied to any email that doesn't set its own From: header.
        # Override per-environment by setting MAILER_FROM.
        envelope:
            sender: '%env(MAILER_FROM)%'
        headers:
            From: '%env(MAILER_FROM)%'
